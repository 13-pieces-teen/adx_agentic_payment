{
  "name": "@agent-arena/settlement-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Mock stablecoin (ERC-20 + EIP-3009) for x402 on Injective EVM — SETTLE-002.5",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "tsx scripts/deploy.ts",
    "deploy:tokens": "tsx scripts/deploy-tokens.ts",
    "deploy:memorial-nft": "tsx scripts/deploy-memorial-nft.ts",
    "generate:memorial-wallet-csv": "tsx scripts/generate-memorial-wallet-csv.ts",
    "issue:memorial-nft": "tsx scripts/issue-memorial-nft.ts",
    "node": "hardhat node",
    "test": "node --test --test-concurrency=1 test/tokens.test.mjs test/memorial-nft.test.mjs && tsx --test test/memorial-wallets.test.ts"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  }
}
