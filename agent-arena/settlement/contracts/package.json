{
  "name": "@agent-arena/settlement-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Mock stablecoin (ERC-20 + EIP-3009) for x402 on Injective EVM — SETTLE-002.5",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "tsx scripts/deploy.ts",
    "deploy:tokens": "tsx scripts/deploy-tokens.ts",
    "node": "hardhat node",
    "test": "node --test test/tokens.test.mjs"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  }
}
