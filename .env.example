# Safe, non-secret defaults only.
APP_ENV=development
INJECTIVE_NETWORK=testnet
INJECTIVE_CHAIN_ID=injective-888

# Add public endpoints and wallet addresses after the runtime stack is frozen.
# Never place private keys, seed phrases, passwords, or access tokens here.
