name: adx

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

x-facilitator-base: &facilitator-base
  profiles:
    - testnet-facilitator
  build:
    context: .
    dockerfile: deploy/docker/Dockerfile.facilitator
  restart: unless-stopped
  init: true
  volumes:
    - ${ADX_FACILITATOR_CSV_HOST_PATH:-./deploy/secrets/arena402-facilitators.csv}:/run/arena402/facilitators.csv:ro
  networks:
    - edge
    - data
  expose:
    - "4021"
  read_only: true
  tmpfs:
    - /tmp:size=32m,mode=1777
  security_opt:
    - no-new-privileges:true
  healthcheck:
    test:
      - CMD
      - node
      - -e
      - >-
        fetch("http://127.0.0.1:4021/health").then(r => {
          if (!r.ok) process.exit(1)
        }).catch(() => process.exit(1))
    interval: 15s
    timeout: 5s
    retries: 10
    start_period: 20s
  stop_grace_period: 30s
  cpus: 0.20
  mem_limit: 256m
  logging: *default-logging

x-api-environment: &api-environment
  ADX_CONNECTOR_DATABASE_URL: postgresql://adx_api_login:${ADX_API_DATABASE_PASSWORD:?Set ADX_API_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
  ADX_HOSTED_CONTROL_DATABASE_URL: postgresql://adx_api_login:${ADX_API_DATABASE_PASSWORD:?Set ADX_API_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
  ADX_ARENA_API_DATABASE_URL: postgresql://adx_api_login:${ADX_API_DATABASE_PASSWORD:?Set ADX_API_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
  DATABASE_URL: postgresql://adx_api_login:${ADX_API_DATABASE_PASSWORD:?Set ADX_API_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
  ADX_CONNECTOR_SESSION_SECRET: ${ADX_CONNECTOR_SESSION_SECRET:?Set ADX_CONNECTOR_SESSION_SECRET in deploy/.env}
  ADX_GITHUB_OAUTH_CLIENT_ID: ${ADX_GITHUB_OAUTH_CLIENT_ID:-}
  ADX_GITHUB_OAUTH_CLIENT_SECRET: ${ADX_GITHUB_OAUTH_CLIENT_SECRET:-}
  ADX_GITHUB_OAUTH_CALLBACK_BASE_URL: ${ADX_GITHUB_OAUTH_CALLBACK_BASE_URL:-}
  ADX_GITHUB_OAUTH_STATE_TTL_SECONDS: ${ADX_GITHUB_OAUTH_STATE_TTL_SECONDS:-600}
  ADX_GITHUB_OAUTH_RELAY_URL: ${ADX_GITHUB_OAUTH_RELAY_URL:-}
  ADX_BOOTSTRAP_INVITE_HASH: ${ADX_BOOTSTRAP_INVITE_HASH:-}
  ADX_CONNECTOR_SESSION_TTL_SECONDS: ${ADX_CONNECTOR_SESSION_TTL_SECONDS:-604800}
  ADX_CONNECTOR_COOKIE_NAME: ${ADX_CONNECTOR_COOKIE_NAME:-adx_session}
  ADX_CONNECTOR_COOKIE_SECURE: ${ADX_CONNECTOR_COOKIE_SECURE:-true}
  ADX_CONNECTOR_AUTH_RATE_LIMIT_ATTEMPTS: ${ADX_CONNECTOR_AUTH_RATE_LIMIT_ATTEMPTS:-10}
  ADX_PUBLIC_REGISTRATION_ENABLED: ${ADX_PUBLIC_REGISTRATION_ENABLED:-false}
  ADX_CONNECTOR_PAIRING_RATE_LIMIT_ATTEMPTS: ${ADX_CONNECTOR_PAIRING_RATE_LIMIT_ATTEMPTS:-60}
  ADX_CONNECTOR_RATE_LIMIT_WINDOW_SECONDS: ${ADX_CONNECTOR_RATE_LIMIT_WINDOW_SECONDS:-60}
  ADX_CONNECTOR_MAX_PENDING_PAIRINGS: ${ADX_CONNECTOR_MAX_PENDING_PAIRINGS:-500}
  ADX_CONNECTOR_UNSAFE_DEMO: "false"
  ADX_ENV: ${ADX_ENV:-production}
  ADX_PUBLIC_APP_URL: ${ADX_PUBLIC_APP_URL:?Set ADX_PUBLIC_APP_URL in deploy/.env}
  ADX_ALLOWED_ORIGINS: ${ADX_PUBLIC_APP_URL:?Set ADX_PUBLIC_APP_URL in deploy/.env}
  ADX_CONNECTOR_MIGRATIONS_DIR: /app/db/migrations
  ADX_HOSTED_AGENTS_ENABLED: ${ADX_HOSTED_AGENTS_ENABLED:-false}
  ADX_ARENA_PARTICIPATION_ENABLED: ${ADX_ARENA_PARTICIPATION_ENABLED:-true}
  ADX_ARENA_CORE_ENABLED: ${ADX_ARENA_CORE_ENABLED:-true}
  ADX_ARENA_MCP_ENABLED: ${ADX_ARENA_MCP_ENABLED:-false}
  ADX_ARENA_MCP_TOKEN_SECRET: ${ADX_ARENA_MCP_TOKEN_SECRET:-}
  ADX_ARENA_PAYMENTS_ENABLED: ${ADX_ARENA_PAYMENTS_ENABLED:-true}
  ADX_ARENA_MEMORIAL_ENABLED: ${ADX_ARENA_MEMORIAL_ENABLED:-false}
  ADX_PUBLIC_API_URL: ${ADX_PUBLIC_API_URL:?Set ADX_PUBLIC_API_URL in deploy/.env}
  ADX_X402_FACILITATOR_ID: ${ADX_X402_FACILITATOR_ID:-}
  ADX_SETTLEMENT_SERVICE_URL: ${ADX_SETTLEMENT_SERVICE_URL:-http://settlement-worker:8790}
  ADX_SETTLEMENT_SERVICE_TOKEN: ${ADX_SETTLEMENT_SERVICE_TOKEN:?Set ADX_SETTLEMENT_SERVICE_TOKEN in deploy/.env}
  ADX_WALLET_RPC_URL: ${ADX_WALLET_RPC_URL:-https://k8s.testnet.json-rpc.injective.network/}
  ADX_WALLET_EXPLORER_URL: ${ADX_WALLET_EXPLORER_URL:-https://testnet.blockscout.injective.network}
  ADX_ARENA_EXPLORER_TX_URL_TEMPLATE: ${ADX_ARENA_EXPLORER_TX_URL_TEMPLATE:-}
  ADX_CURRENT_GAME_CHAIN_ID: ${ADX_CURRENT_GAME_CHAIN_ID:-1439}
  ADX_ARENA402_G_TOKEN_ADDRESS: ${ADX_ARENA402_G_TOKEN_ADDRESS:-0xBF7B7268CE82d92BaC7a95a741F4003FE84e1884}
  ADX_ARENA402_G_TOKEN_DECIMALS: ${ADX_ARENA402_G_TOKEN_DECIMALS:-6}
  ADX_ARENA402_M_TOKEN_ADDRESS: ${ADX_ARENA402_M_TOKEN_ADDRESS:-}
  ADX_ARENA402_M_TOKEN_DECIMALS: ${ADX_ARENA402_M_TOKEN_DECIMALS:-6}
  ADX_ARENA_ADMIN_GITHUB_SUBJECTS: ${ADX_ARENA_ADMIN_GITHUB_SUBJECTS:-}
  ADX_ARENA_CORE_DATABASE_URL: postgresql://adx_arena_core_login:${ADX_ARENA_CORE_DATABASE_PASSWORD:?Set ADX_ARENA_CORE_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
  ADX_HOSTED_FINGERPRINT_PEPPER_B64: ${ADX_HOSTED_FINGERPRINT_PEPPER_B64:-}
  ADX_HOSTED_FINGERPRINT_PEPPER_VERSION: ${ADX_HOSTED_FINGERPRINT_PEPPER_VERSION:-1}
  ADX_HOSTED_SECRET_BACKEND: ${ADX_HOSTED_SECRET_BACKEND:-tencent_ssm}
  ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED: ${ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED:-false}
  ADX_HOSTED_MASTER_KEY_FILE: /run/secrets/arena402/hosted-master.key
  ADX_HOSTED_MASTER_KEY_VERSION: ${ADX_HOSTED_MASTER_KEY_VERSION:-1}
  ADX_TENCENT_SSM_REGION: ${ADX_TENCENT_SSM_REGION:-ap-guangzhou}
  ADX_TENCENT_SSM_RECOVERY_WINDOW_DAYS: ${ADX_TENCENT_SSM_RECOVERY_WINDOW_DAYS:-0}
  ADX_TENCENT_SSM_IAM_VERIFIED: ${ADX_TENCENT_SSM_IAM_VERIFIED:-false}
  TENCENTCLOUD_SECRET_ID: ${ADX_TENCENT_SSM_WRITER_SECRET_ID:-}
  TENCENTCLOUD_SECRET_KEY: ${ADX_TENCENT_SSM_WRITER_SECRET_KEY:-}
  TENCENTCLOUD_TOKEN: ${ADX_TENCENT_SSM_WRITER_TOKEN:-}
  TZ: ${TZ:-Asia/Shanghai}

services:
  postgres:
    image: postgres:17-alpine3.22@sha256:b02d9b5bcf608c2719da32cdabee274a33841202487fd5dc9b065b63f886753f
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-adx}
      POSTGRES_USER: ${POSTGRES_USER:-adx}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in deploy/.env}
      PGDATA: /var/lib/postgresql/data/pgdata
    command:
      - postgres
      - -c
      - shared_buffers=192MB
      - -c
      - effective_cache_size=512MB
      - -c
      - maintenance_work_mem=32MB
      - -c
      - work_mem=4MB
      - -c
      - max_connections=${POSTGRES_MAX_CONNECTIONS:-120}
      - -c
      - wal_compression=on
      - -c
      - log_min_duration_statement=1000
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    security_opt:
      - no-new-privileges:true
    shm_size: 128mb
    stop_grace_period: 1m
    cpus: 0.50
    mem_limit: 768m
    logging: *default-logging

  migrate:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    restart: "no"
    command:
      ["python", "/app/deploy/scripts/migrate.py", "--scope", "all"]
    environment:
      ADX_CONNECTOR_DATABASE_URL: postgresql://${POSTGRES_USER:-adx}:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      DATABASE_URL: postgresql://${POSTGRES_USER:-adx}:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_CONNECTOR_MIGRATIONS_DIR: /app/db/migrations
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - data
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    cpus: 0.50
    mem_limit: 512m
    logging: *default-logging

  provision-db-roles:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    restart: "no"
    command: ["python", "/app/deploy/scripts/provision_db_roles.py"]
    environment:
      ADX_DATABASE_ADMIN_URL: postgresql://${POSTGRES_USER:-adx}:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_API_DATABASE_PASSWORD: ${ADX_API_DATABASE_PASSWORD:?Set ADX_API_DATABASE_PASSWORD in deploy/.env}
      ADX_HOSTED_WORKER_DATABASE_PASSWORD: ${ADX_HOSTED_WORKER_DATABASE_PASSWORD:?Set ADX_HOSTED_WORKER_DATABASE_PASSWORD in deploy/.env}
      ADX_ARENA_CORE_DATABASE_PASSWORD: ${ADX_ARENA_CORE_DATABASE_PASSWORD:?Set ADX_ARENA_CORE_DATABASE_PASSWORD in deploy/.env}
      ADX_SETTLEMENT_DATABASE_PASSWORD: ${ADX_SETTLEMENT_DATABASE_PASSWORD:?Set ADX_SETTLEMENT_DATABASE_PASSWORD in deploy/.env}
      ADX_CREDENTIAL_CONTROLLER_DATABASE_PASSWORD: ${ADX_CREDENTIAL_CONTROLLER_DATABASE_PASSWORD:?Set ADX_CREDENTIAL_CONTROLLER_DATABASE_PASSWORD in deploy/.env}
      ADX_WALLET_SIGNER_DATABASE_PASSWORD: ${ADX_WALLET_SIGNER_DATABASE_PASSWORD:?Set ADX_WALLET_SIGNER_DATABASE_PASSWORD in deploy/.env}
      ADX_WALLET_IMPORTER_DATABASE_PASSWORD: ${ADX_WALLET_IMPORTER_DATABASE_PASSWORD:?Set ADX_WALLET_IMPORTER_DATABASE_PASSWORD in deploy/.env}
    depends_on:
      migrate:
        condition: service_completed_successfully
    networks:
      - data
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    cpus: 0.25
    mem_limit: 256m
    logging: *default-logging

  api:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    restart: unless-stopped
    init: true
    environment:
      <<: *api-environment
      ADX_API_MAX_CONCURRENCY: ${ADX_API_MAX_CONCURRENCY:-256}
      ADX_API_DB_POOL_MAX_SIZE: ${ADX_API_DB_POOL_MAX_SIZE:-3}
      ADX_API_WORKERS: ${ADX_API_WORKERS:-2}
      ADX_CONNECTOR_SURFACE_ENABLED: "false"
      ADX_ARENA_MCP_ENABLED: "false"
      ADX_FORWARDED_ALLOW_IPS: "*"
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - edge
      - data
    volumes:
      - type: bind
        source: ${ADX_HOSTED_SECRET_DIR_HOST_PATH:-./deploy/secrets}
        target: /run/secrets/arena402
        read_only: true
    expose:
      - "8000"
    read_only: true
    tmpfs:
      - /tmp:size=128m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen("http://127.0.0.1:8000/api/ready", timeout=3)
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
    stop_grace_period: 30s
    cpus: 0.50
    mem_limit: 512m
    logging: *default-logging

  connector-api:
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    restart: unless-stopped
    init: true
    environment:
      <<: *api-environment
      ADX_ASGI_APP: web.connector_api:create_app
      ADX_API_WORKERS: "1"
      ADX_API_MAX_CONCURRENCY: ${ADX_CONNECTOR_MAX_CONCURRENCY:-512}
      ADX_API_DB_POOL_MAX_SIZE: ${ADX_API_DB_POOL_MAX_SIZE:-3}
      ADX_FORWARDED_ALLOW_IPS: "*"
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - edge
      - data
    expose:
      - "8000"
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen("http://127.0.0.1:8000/api/ready", timeout=3)
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
    stop_grace_period: 30s
    cpus: 0.40
    mem_limit: 384m
    logging: *default-logging

  hosted-worker:
    profiles:
      - hosted
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    restart: unless-stopped
    init: true
    command: ["python", "-m", "hosted_agent_runtime.production_worker"]
    environment:
      ADX_HOSTED_WORKER_DATABASE_URL: postgresql://adx_hosted_worker_login:${ADX_HOSTED_WORKER_DATABASE_PASSWORD:?Set ADX_HOSTED_WORKER_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_HOSTED_SECRET_BACKEND: ${ADX_HOSTED_SECRET_BACKEND:-tencent_ssm}
      ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED: ${ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED:-false}
      ADX_HOSTED_MASTER_KEY_FILE: /run/secrets/arena402/hosted-master.key
      ADX_HOSTED_MASTER_KEY_VERSION: ${ADX_HOSTED_MASTER_KEY_VERSION:-1}
      ADX_TENCENT_SSM_REGION: ${ADX_TENCENT_SSM_REGION:-ap-guangzhou}
      ADX_TENCENT_SSM_IAM_VERIFIED: ${ADX_TENCENT_SSM_IAM_VERIFIED:-false}
      TENCENTCLOUD_SECRET_ID: ${ADX_TENCENT_SSM_READER_SECRET_ID:-}
      TENCENTCLOUD_SECRET_KEY: ${ADX_TENCENT_SSM_READER_SECRET_KEY:-}
      TENCENTCLOUD_TOKEN: ${ADX_TENCENT_SSM_READER_TOKEN:-}
      ADX_OPENAI_COMPATIBLE_ENDPOINT: ${ADX_OPENAI_COMPATIBLE_ENDPOINT:-}
      ADX_OPENAI_COMPATIBLE_MODELS: ${ADX_OPENAI_COMPATIBLE_MODELS:-}
      ADX_HOSTED_WORKER_LEASE_SECONDS: ${ADX_HOSTED_WORKER_LEASE_SECONDS:-600}
      ADX_HOSTED_WORKER_POLL_SECONDS: ${ADX_HOSTED_WORKER_POLL_SECONDS:-1}
      ADX_HOSTED_WORKER_TASK_CONCURRENCY: ${ADX_HOSTED_WORKER_TASK_CONCURRENCY:-8}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - edge
      - data
      - official-llm
    volumes:
      - type: bind
        source: ${ADX_HOSTED_SECRET_DIR_HOST_PATH:-./deploy/secrets}
        target: /run/secrets/arena402
        read_only: true
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    cpus: 0.45
    mem_limit: 512m
    logging: *default-logging

  credential-controller:
    profiles:
      - hosted
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    restart: unless-stopped
    init: true
    command:
      ["python", "-m", "hosted_agent_control_plane.production_controller"]
    environment:
      ADX_CREDENTIAL_CONTROLLER_DATABASE_URL: postgresql://adx_credential_controller_login:${ADX_CREDENTIAL_CONTROLLER_DATABASE_PASSWORD:?Set ADX_CREDENTIAL_CONTROLLER_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_HOSTED_SECRET_BACKEND: ${ADX_HOSTED_SECRET_BACKEND:-tencent_ssm}
      ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED: ${ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED:-false}
      ADX_TENCENT_SSM_REGION: ${ADX_TENCENT_SSM_REGION:-ap-guangzhou}
      ADX_TENCENT_SSM_RECOVERY_WINDOW_DAYS: ${ADX_TENCENT_SSM_RECOVERY_WINDOW_DAYS:-0}
      ADX_TENCENT_SSM_IAM_VERIFIED: ${ADX_TENCENT_SSM_IAM_VERIFIED:-false}
      TENCENTCLOUD_SECRET_ID: ${ADX_TENCENT_SSM_CONTROLLER_SECRET_ID:-}
      TENCENTCLOUD_SECRET_KEY: ${ADX_TENCENT_SSM_CONTROLLER_SECRET_KEY:-}
      TENCENTCLOUD_TOKEN: ${ADX_TENCENT_SSM_CONTROLLER_TOKEN:-}
      ADX_CREDENTIAL_CONTROLLER_LEASE_SECONDS: ${ADX_CREDENTIAL_CONTROLLER_LEASE_SECONDS:-60}
      ADX_CREDENTIAL_CONTROLLER_POLL_SECONDS: ${ADX_CREDENTIAL_CONTROLLER_POLL_SECONDS:-1}
      ADX_CREDENTIAL_CONTROLLER_RETRY_SECONDS: ${ADX_CREDENTIAL_CONTROLLER_RETRY_SECONDS:-30}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - edge
      - data
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    cpus: 0.10
    mem_limit: 192m
    logging: *default-logging

  official-litellm:
    profiles:
      - official-agents
      - ops
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.litellm
    restart: unless-stopped
    init: true
    environment:
      ADX_LITELLM_SECRET_DATABASE_URL: postgresql://adx_hosted_worker_login:${ADX_HOSTED_WORKER_DATABASE_PASSWORD:?Set ADX_HOSTED_WORKER_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_HOSTED_SECRET_BACKEND: ${ADX_HOSTED_SECRET_BACKEND:-tencent_ssm}
      ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED: ${ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED:-false}
      ADX_HOSTED_MASTER_KEY_FILE: /run/secrets/arena402/hosted-master.key
      ADX_HOSTED_MASTER_KEY_VERSION: ${ADX_HOSTED_MASTER_KEY_VERSION:-1}
      ADX_TENCENT_SSM_REGION: ${ADX_TENCENT_SSM_REGION:-ap-guangzhou}
      ADX_TENCENT_SSM_IAM_VERIFIED: ${ADX_TENCENT_SSM_IAM_VERIFIED:-false}
      TENCENTCLOUD_SECRET_ID: ${ADX_TENCENT_SSM_READER_SECRET_ID:-}
      TENCENTCLOUD_SECRET_KEY: ${ADX_TENCENT_SSM_READER_SECRET_KEY:-}
      TENCENTCLOUD_TOKEN: ${ADX_TENCENT_SSM_READER_TOKEN:-}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - data
      - official-llm
    expose:
      - "4000"
    volumes:
      - type: bind
        source: ${ADX_HOSTED_SECRET_DIR_HOST_PATH:-./deploy/secrets}
        target: /run/secrets/arena402
        read_only: true
      - type: bind
        source: ${ADX_OFFICIAL_LITELLM_CONFIG_HOST_PATH:-./deploy/official-litellm}
        target: /run/official-litellm
        read_only: true
        bind:
          create_host_path: false
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen(
            "http://127.0.0.1:4000/health/readiness", timeout=3
          )
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 60s
    stop_grace_period: 30s
    cpus: 0.40
    mem_limit: 768m
    logging: *default-logging

  official-litellm-provision:
    profiles:
      - ops
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    init: true
    command:
      - python
      - -m
      - scripts.provision_official_litellm
      - --deepseek-key-source
      - /run/secrets/official/deepseek-source
      - --litellm-token-file
      - /run/secrets/official/litellm-token.key
      - --manifest-file
      - /run/official-litellm/manifest.json
      - --model-alias
      - ${ADX_OFFICIAL_AGENT_MODEL:-deepseek-v4-flash}
      - --upstream-model
      - ${ADX_OFFICIAL_DEEPSEEK_UPSTREAM_MODEL:-deepseek-chat}
      - --config-version
      - ${ADX_OFFICIAL_LITELLM_CONFIG_VERSION:-v1}
    environment:
      ADX_HOSTED_CONTROL_DATABASE_URL: postgresql://adx_api_login:${ADX_API_DATABASE_PASSWORD:?Set ADX_API_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_HOSTED_SECRET_BACKEND: ${ADX_HOSTED_SECRET_BACKEND:-tencent_ssm}
      ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED: ${ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED:-false}
      ADX_HOSTED_MASTER_KEY_FILE: /run/secrets/arena402/hosted-master.key
      ADX_HOSTED_MASTER_KEY_VERSION: ${ADX_HOSTED_MASTER_KEY_VERSION:-1}
      ADX_TENCENT_SSM_REGION: ${ADX_TENCENT_SSM_REGION:-ap-guangzhou}
      ADX_TENCENT_SSM_IAM_VERIFIED: ${ADX_TENCENT_SSM_IAM_VERIFIED:-false}
      TENCENTCLOUD_SECRET_ID: ${ADX_TENCENT_SSM_WRITER_SECRET_ID:-}
      TENCENTCLOUD_SECRET_KEY: ${ADX_TENCENT_SSM_WRITER_SECRET_KEY:-}
      TENCENTCLOUD_TOKEN: ${ADX_TENCENT_SSM_WRITER_TOKEN:-}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - data
    volumes:
      - type: bind
        source: ${ADX_HOSTED_SECRET_DIR_HOST_PATH:-./deploy/secrets}
        target: /run/secrets/arena402
        read_only: true
      - type: bind
        source: ${ADX_OFFICIAL_DEEPSEEK_KEY_SOURCE_HOST_PATH:-./deploy/official-agent-keys}
        target: /run/secrets/official/deepseek-source
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${ADX_OFFICIAL_LITELLM_TOKEN_FILE_HOST_PATH:-./deploy/secrets/official-litellm-token.key}
        target: /run/secrets/official/litellm-token.key
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${ADX_OFFICIAL_LITELLM_CONFIG_HOST_PATH:-./deploy/official-litellm}
        target: /run/official-litellm
        bind:
          create_host_path: false
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  official-agent-bootstrap:
    profiles:
      - ops
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    init: true
    command:
      - python
      - -m
      - scripts.bootstrap_official_agent_pool
      - --litellm-token-file
      - /run/secrets/official/litellm-token.key
      - --count
      - ${ADX_OFFICIAL_AGENT_COUNT:-20}
      - --model
      - ${ADX_OFFICIAL_AGENT_MODEL:-deepseek-v4-flash}
      - --config-version
      - ${ADX_OFFICIAL_LITELLM_CONFIG_VERSION:-v1}
      - --activate
      - --replace-enabled-pool
    environment:
      ADX_HOSTED_CONTROL_DATABASE_URL: postgresql://adx_api_login:${ADX_API_DATABASE_PASSWORD:?Set ADX_API_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_OFFICIAL_BOOTSTRAP_DATABASE_URL: postgresql://${POSTGRES_USER:-adx}:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_HOSTED_FINGERPRINT_PEPPER_B64: ${ADX_HOSTED_FINGERPRINT_PEPPER_B64:?Set ADX_HOSTED_FINGERPRINT_PEPPER_B64 in deploy/.env}
      ADX_HOSTED_FINGERPRINT_PEPPER_VERSION: ${ADX_HOSTED_FINGERPRINT_PEPPER_VERSION:-1}
      ADX_HOSTED_SECRET_BACKEND: ${ADX_HOSTED_SECRET_BACKEND:-tencent_ssm}
      ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED: ${ADX_HOSTED_CREDENTIAL_BACKEND_VERIFIED:-false}
      ADX_HOSTED_MASTER_KEY_FILE: /run/secrets/arena402/hosted-master.key
      ADX_HOSTED_MASTER_KEY_VERSION: ${ADX_HOSTED_MASTER_KEY_VERSION:-1}
      ADX_TENCENT_SSM_REGION: ${ADX_TENCENT_SSM_REGION:-ap-guangzhou}
      ADX_TENCENT_SSM_IAM_VERIFIED: ${ADX_TENCENT_SSM_IAM_VERIFIED:-false}
      TENCENTCLOUD_SECRET_ID: ${ADX_TENCENT_SSM_WRITER_SECRET_ID:-}
      TENCENTCLOUD_SECRET_KEY: ${ADX_TENCENT_SSM_WRITER_SECRET_KEY:-}
      TENCENTCLOUD_TOKEN: ${ADX_TENCENT_SSM_WRITER_TOKEN:-}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
      official-litellm:
        condition: service_healthy
    networks:
      - data
    volumes:
      - type: bind
        source: ${ADX_HOSTED_SECRET_DIR_HOST_PATH:-./deploy/secrets}
        target: /run/secrets/arena402
        read_only: true
      - type: bind
        source: ${ADX_OFFICIAL_LITELLM_TOKEN_FILE_HOST_PATH:-./deploy/secrets/official-litellm-token.key}
        target: /run/secrets/official/litellm-token.key
        read_only: true
        bind:
          create_host_path: false
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  official-agent-strategy-refresh:
    profiles:
      - ops
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    init: true
    command:
      - python
      - -m
      - scripts.refresh_official_agent_strategies
    environment:
      ADX_HOSTED_CONTROL_DATABASE_URL: postgresql://adx_api_login:${ADX_API_DATABASE_PASSWORD:?Set ADX_API_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_OFFICIAL_BOOTSTRAP_DATABASE_URL: postgresql://${POSTGRES_USER:-adx}:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - data
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  arena-worker:
    profiles:
      - arena
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    restart: unless-stopped
    init: true
    command: ["python", "-m", "arena_game.production_worker"]
    environment:
      ADX_ARENA_CORE_DATABASE_URL: postgresql://adx_arena_core_login:${ADX_ARENA_CORE_DATABASE_PASSWORD:?Set ADX_ARENA_CORE_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_ARENA_SETTLEMENT_RPC_URL: ${ADX_ARENA_SETTLEMENT_RPC_URL:?Set ADX_ARENA_SETTLEMENT_RPC_URL in deploy/.env}
      ADX_ARENA_SETTLEMENT_BLOCKSCOUT_URL: ${ADX_ARENA_SETTLEMENT_BLOCKSCOUT_URL:-}
      ADX_ARENA_WORKER_LEASE_SECONDS: ${ADX_ARENA_WORKER_LEASE_SECONDS:-600}
      ADX_ARENA_WORKER_POLL_SECONDS: ${ADX_ARENA_WORKER_POLL_SECONDS:-0.25}
      ADX_ARENA_ORCHESTRATION_POLL_SECONDS: ${ADX_ARENA_ORCHESTRATION_POLL_SECONDS:-0.25}
      ADX_ARENA_FINALIZER_POLL_SECONDS: ${ADX_ARENA_FINALIZER_POLL_SECONDS:-1}
      ADX_SETTLEMENT_RECOVERY_POLL_SECONDS: ${ADX_SETTLEMENT_RECOVERY_POLL_SECONDS:-3}
      ADX_CURRENT_GAME_POLL_SECONDS: ${ADX_CURRENT_GAME_POLL_SECONDS:-1}
      ADX_CURRENT_GAME_ROUND_COUNT: ${ADX_CURRENT_GAME_ROUND_COUNT:-8}
      ADX_CURRENT_GAME_START_THRESHOLD: ${ADX_CURRENT_GAME_START_THRESHOLD:-10}
      ADX_CURRENT_GAME_MAX_PARTICIPANTS: ${ADX_CURRENT_GAME_MAX_PARTICIPANTS:-100}
      ADX_CURRENT_GAME_OFFICIAL_FILL_AFTER_SECONDS: ${ADX_CURRENT_GAME_OFFICIAL_FILL_AFTER_SECONDS:-300}
      ADX_CURRENT_GAME_ACTION_TIMEOUT_MS: ${ADX_CURRENT_GAME_ACTION_TIMEOUT_MS:-90000}
      ADX_CURRENT_GAME_MAX_NEGOTIATION_TURNS: ${ADX_CURRENT_GAME_MAX_NEGOTIATION_TURNS:-3}
      ADX_CURRENT_GAME_CHAIN_ID: ${ADX_CURRENT_GAME_CHAIN_ID:-1439}
      ADX_CURRENT_GAME_TOKEN_ADDRESS: ${ADX_CURRENT_GAME_TOKEN_ADDRESS:-0xBF7B7268CE82d92BaC7a95a741F4003FE84e1884}
      ADX_CURRENT_GAME_TOKEN_SYMBOL: ${ADX_CURRENT_GAME_TOKEN_SYMBOL:-arena402-g}
      ADX_CURRENT_GAME_TOKEN_DECIMALS: ${ADX_CURRENT_GAME_TOKEN_DECIMALS:-6}
      ADX_CURRENT_GAME_TOKEN_EIP712_NAME: ${ADX_CURRENT_GAME_TOKEN_EIP712_NAME:-Arena 402 Gold}
      ADX_CURRENT_GAME_TOKEN_EIP712_VERSION: ${ADX_CURRENT_GAME_TOKEN_EIP712_VERSION:-1}
      ADX_CURRENT_GAME_REQUIRED_CONFIRMATIONS: ${ADX_CURRENT_GAME_REQUIRED_CONFIRMATIONS:-2}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - edge
      - data
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    cpus: 0.30
    mem_limit: 320m
    logging: *default-logging

  settlement-worker:
    profiles:
      - settlement
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.api
    restart: unless-stopped
    init: true
    command:
      [
        "python",
        "-m",
        "uvicorn",
        "arena_payments.production_service:create_app",
        "--factory",
        "--host",
        "0.0.0.0",
        "--port",
        "8790",
        "--no-access-log",
      ]
    environment:
      ADX_SETTLEMENT_DATABASE_URL: postgresql://adx_settlement_login:${ADX_SETTLEMENT_DATABASE_PASSWORD:?Set ADX_SETTLEMENT_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_ARENA_AUTOMATIC_PAYMENTS_ENABLED: ${ADX_ARENA_AUTOMATIC_PAYMENTS_ENABLED:-false}
      ADX_SETTLEMENT_INTENT_ID: ${ADX_SETTLEMENT_INTENT_ID:-}
      ADX_ARENA_SETTLEMENT_RPC_URL: ${ADX_ARENA_SETTLEMENT_RPC_URL:?Set ADX_ARENA_SETTLEMENT_RPC_URL in deploy/.env}
      ADX_ARENA_SETTLEMENT_BLOCKSCOUT_URL: ${ADX_ARENA_SETTLEMENT_BLOCKSCOUT_URL:?Set ADX_ARENA_SETTLEMENT_BLOCKSCOUT_URL in deploy/.env}
      ADX_PUBLIC_API_URL: ${ADX_PUBLIC_API_URL:?Set ADX_PUBLIC_API_URL in deploy/.env}
      ADX_X402_FACILITATOR_SHARD_COUNT: ${ADX_X402_FACILITATOR_SHARD_COUNT:-4}
      ADX_X402_FACILITATOR_1_URL: ${ADX_X402_FACILITATOR_1_URL:-http://arena-facilitator-1:4021}
      ADX_X402_FACILITATOR_1_ID: ${ADX_X402_FACILITATOR_1_ID:-arena402-injective-testnet-1}
      ADX_X402_FACILITATOR_1_EOA: ${ADX_X402_FACILITATOR_1_EOA:-}
      ADX_X402_FACILITATOR_1_AUTHORIZATION: ${ADX_X402_FACILITATOR_1_AUTHORIZATION:-}
      ADX_X402_FACILITATOR_2_URL: ${ADX_X402_FACILITATOR_2_URL:-http://arena-facilitator-2:4021}
      ADX_X402_FACILITATOR_2_ID: ${ADX_X402_FACILITATOR_2_ID:-arena402-injective-testnet-2}
      ADX_X402_FACILITATOR_2_EOA: ${ADX_X402_FACILITATOR_2_EOA:-}
      ADX_X402_FACILITATOR_2_AUTHORIZATION: ${ADX_X402_FACILITATOR_2_AUTHORIZATION:-}
      ADX_X402_FACILITATOR_3_URL: ${ADX_X402_FACILITATOR_3_URL:-http://arena-facilitator-3:4021}
      ADX_X402_FACILITATOR_3_ID: ${ADX_X402_FACILITATOR_3_ID:-arena402-injective-testnet-3}
      ADX_X402_FACILITATOR_3_EOA: ${ADX_X402_FACILITATOR_3_EOA:-}
      ADX_X402_FACILITATOR_3_AUTHORIZATION: ${ADX_X402_FACILITATOR_3_AUTHORIZATION:-}
      ADX_X402_FACILITATOR_4_URL: ${ADX_X402_FACILITATOR_4_URL:-http://arena-facilitator-4:4021}
      ADX_X402_FACILITATOR_4_ID: ${ADX_X402_FACILITATOR_4_ID:-arena402-injective-testnet-4}
      ADX_X402_FACILITATOR_4_EOA: ${ADX_X402_FACILITATOR_4_EOA:-}
      ADX_X402_FACILITATOR_4_AUTHORIZATION: ${ADX_X402_FACILITATOR_4_AUTHORIZATION:-}
      ADX_WALLET_SIGNER_URL: ${ADX_WALLET_SIGNER_URL:?Set ADX_WALLET_SIGNER_URL in deploy/.env}
      ADX_WALLET_SIGNER_TOKEN: ${ADX_WALLET_SIGNER_TOKEN:?Set ADX_WALLET_SIGNER_TOKEN in deploy/.env}
      ADX_SETTLEMENT_SERVICE_TOKEN: ${ADX_SETTLEMENT_SERVICE_TOKEN:?Set ADX_SETTLEMENT_SERVICE_TOKEN in deploy/.env}
      ADX_SETTLEMENT_WORKER_POLL_SECONDS: ${ADX_SETTLEMENT_WORKER_POLL_SECONDS:-0.5}
      ADX_SETTLEMENT_WORKER_CONCURRENCY: ${ADX_SETTLEMENT_WORKER_CONCURRENCY:-4}
      ADX_SETTLEMENT_LEASE_SECONDS: ${ADX_SETTLEMENT_LEASE_SECONDS:-60}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    networks:
      - edge
      - data
    expose:
      - "8790"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen("http://127.0.0.1:8790/health", timeout=3)
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  wallet-signer:
    profiles:
      - testnet-signer
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.wallet-signer
    restart: unless-stopped
    init: true
    environment:
      ADX_WALLET_SIGNER_BACKEND: postgres_aesgcm
      ADX_WALLET_SIGNER_DATABASE_URL: postgresql://adx_wallet_signer_login:${ADX_WALLET_SIGNER_DATABASE_PASSWORD:?Set ADX_WALLET_SIGNER_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_WALLET_MASTER_KEY_FILE: /run/secrets/wallet-vault/wallet-master.key
      ADX_WALLET_MASTER_KEY_VERSION: ${ADX_WALLET_MASTER_KEY_VERSION:-1}
      ADX_WALLET_SIGNER_TOKEN: ${ADX_WALLET_SIGNER_TOKEN:-}
      ADX_WALLET_SIGNER_ALLOWED_CHAIN_ID: ${ADX_WALLET_SIGNER_ALLOWED_CHAIN_ID:-1439}
      ADX_WALLET_SIGNER_PORT: "8787"
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    volumes:
      - type: bind
        source: ${ADX_WALLET_SECRET_DIR_HOST_PATH:-./deploy/secrets}
        target: /run/secrets/wallet-vault
        read_only: true
    networks:
      - data
    expose:
      - "8787"
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - >-
          fetch("http://127.0.0.1:8787/health").then(r => {
            if (!r.ok) process.exit(1)
          }).catch(() => process.exit(1))
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s
    stop_grace_period: 15s
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  wallet-vault-admin:
    profiles:
      - wallet-admin
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.wallet-signer
    restart: "no"
    command: ["npm", "run", "wallet:vault-import"]
    environment:
      ADX_WALLET_VAULT_DATABASE_URL: postgresql://adx_wallet_importer_login:${ADX_WALLET_IMPORTER_DATABASE_PASSWORD:?Set ADX_WALLET_IMPORTER_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_WALLET_VAULT_IMPORT_CSV_PATH: /run/import/agent-wallets.csv
      ADX_WALLET_MASTER_KEY_FILE: /run/secrets/wallet-vault/wallet-master.key
      ADX_WALLET_MASTER_KEY_VERSION: ${ADX_WALLET_MASTER_KEY_VERSION:-1}
      ADX_WALLET_SIGNER_ALLOWED_CHAIN_ID: ${ADX_WALLET_SIGNER_ALLOWED_CHAIN_ID:-1439}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    volumes:
      - type: bind
        source: ${ADX_WALLET_SECRET_DIR_HOST_PATH:-./deploy/secrets}
        target: /run/secrets/wallet-vault
        read_only: true
      - type: bind
        source: ${ADX_WALLET_IMPORT_CSV_HOST_PATH:-./deploy/secrets/agent-wallets.csv}
        target: /run/import/agent-wallets.csv
        read_only: true
    networks:
      - data
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  wallet-vault-rotate:
    profiles:
      - wallet-admin
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.wallet-signer
    restart: "no"
    command: ["npm", "run", "wallet:vault-rotate"]
    environment:
      ADX_WALLET_VAULT_DATABASE_URL: postgresql://adx_wallet_importer_login:${ADX_WALLET_IMPORTER_DATABASE_PASSWORD:?Set ADX_WALLET_IMPORTER_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_WALLET_OLD_MASTER_KEY_FILE: /run/secrets/wallet-vault/${ADX_WALLET_OLD_MASTER_KEY_FILENAME:-wallet-master.key}
      ADX_WALLET_NEW_MASTER_KEY_FILE: /run/secrets/wallet-vault/${ADX_WALLET_NEW_MASTER_KEY_FILENAME:-wallet-master-next.key}
      ADX_WALLET_OLD_MASTER_KEY_VERSION: ${ADX_WALLET_OLD_MASTER_KEY_VERSION:-1}
      ADX_WALLET_NEW_MASTER_KEY_VERSION: ${ADX_WALLET_NEW_MASTER_KEY_VERSION:-2}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    volumes:
      - type: bind
        source: ${ADX_WALLET_SECRET_DIR_HOST_PATH:-./deploy/secrets}
        target: /run/secrets/wallet-vault
        read_only: true
    networks:
      - data
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  arena-facilitator-1:
    <<: *facilitator-base
    environment:
      ADX_ENV: production
      ADX_SETTLEMENT_DEPLOYMENTS_PATH: /app/deployments.json
      ADX_ARENA_SETTLEMENT_RPC_URL: ${ADX_ARENA_SETTLEMENT_RPC_URL:?Set ADX_ARENA_SETTLEMENT_RPC_URL in deploy/.env}
      ADX_FACILITATOR_CSV_PATH: /run/arena402/facilitators.csv
      ADX_FACILITATOR_WALLET_INDEX: ${ADX_FACILITATOR_1_WALLET_INDEX:-1}
      ADX_X402_FACILITATOR_BEARER_TOKEN: ${ADX_X402_FACILITATOR_1_BEARER_TOKEN:-}
      ADX_FACILITATOR_ALLOWED_RESOURCE_ORIGIN: ${ADX_PUBLIC_API_URL:?Set ADX_PUBLIC_API_URL in deploy/.env}
      ADX_X402_SETTLEMENT_TOKEN_ADDRESS: ${ADX_CURRENT_GAME_TOKEN_ADDRESS:-0xBF7B7268CE82d92BaC7a95a741F4003FE84e1884}
      FACILITATOR_PORT: "4021"
      TZ: ${TZ:-Asia/Shanghai}

  arena-facilitator-2:
    <<: *facilitator-base
    environment:
      ADX_ENV: production
      ADX_SETTLEMENT_DEPLOYMENTS_PATH: /app/deployments.json
      ADX_ARENA_SETTLEMENT_RPC_URL: ${ADX_ARENA_SETTLEMENT_RPC_URL:?Set ADX_ARENA_SETTLEMENT_RPC_URL in deploy/.env}
      ADX_FACILITATOR_CSV_PATH: /run/arena402/facilitators.csv
      ADX_FACILITATOR_WALLET_INDEX: ${ADX_FACILITATOR_2_WALLET_INDEX:-2}
      ADX_X402_FACILITATOR_BEARER_TOKEN: ${ADX_X402_FACILITATOR_2_BEARER_TOKEN:-}
      ADX_FACILITATOR_ALLOWED_RESOURCE_ORIGIN: ${ADX_PUBLIC_API_URL:?Set ADX_PUBLIC_API_URL in deploy/.env}
      ADX_X402_SETTLEMENT_TOKEN_ADDRESS: ${ADX_CURRENT_GAME_TOKEN_ADDRESS:-0xBF7B7268CE82d92BaC7a95a741F4003FE84e1884}
      FACILITATOR_PORT: "4021"
      TZ: ${TZ:-Asia/Shanghai}

  arena-facilitator-3:
    <<: *facilitator-base
    environment:
      ADX_ENV: production
      ADX_SETTLEMENT_DEPLOYMENTS_PATH: /app/deployments.json
      ADX_ARENA_SETTLEMENT_RPC_URL: ${ADX_ARENA_SETTLEMENT_RPC_URL:?Set ADX_ARENA_SETTLEMENT_RPC_URL in deploy/.env}
      ADX_FACILITATOR_CSV_PATH: /run/arena402/facilitators.csv
      ADX_FACILITATOR_WALLET_INDEX: ${ADX_FACILITATOR_3_WALLET_INDEX:-3}
      ADX_X402_FACILITATOR_BEARER_TOKEN: ${ADX_X402_FACILITATOR_3_BEARER_TOKEN:-}
      ADX_FACILITATOR_ALLOWED_RESOURCE_ORIGIN: ${ADX_PUBLIC_API_URL:?Set ADX_PUBLIC_API_URL in deploy/.env}
      ADX_X402_SETTLEMENT_TOKEN_ADDRESS: ${ADX_CURRENT_GAME_TOKEN_ADDRESS:-0xBF7B7268CE82d92BaC7a95a741F4003FE84e1884}
      FACILITATOR_PORT: "4021"
      TZ: ${TZ:-Asia/Shanghai}

  arena-facilitator-4:
    <<: *facilitator-base
    environment:
      ADX_ENV: production
      ADX_SETTLEMENT_DEPLOYMENTS_PATH: /app/deployments.json
      ADX_ARENA_SETTLEMENT_RPC_URL: ${ADX_ARENA_SETTLEMENT_RPC_URL:?Set ADX_ARENA_SETTLEMENT_RPC_URL in deploy/.env}
      ADX_FACILITATOR_CSV_PATH: /run/arena402/facilitators.csv
      ADX_FACILITATOR_WALLET_INDEX: ${ADX_FACILITATOR_4_WALLET_INDEX:-4}
      ADX_X402_FACILITATOR_BEARER_TOKEN: ${ADX_X402_FACILITATOR_4_BEARER_TOKEN:-}
      ADX_FACILITATOR_ALLOWED_RESOURCE_ORIGIN: ${ADX_PUBLIC_API_URL:?Set ADX_PUBLIC_API_URL in deploy/.env}
      ADX_X402_SETTLEMENT_TOKEN_ADDRESS: ${ADX_CURRENT_GAME_TOKEN_ADDRESS:-0xBF7B7268CE82d92BaC7a95a741F4003FE84e1884}
      FACILITATOR_PORT: "4021"
      TZ: ${TZ:-Asia/Shanghai}

  gamecoin-provisioner:
    profiles:
      - gamecoin
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.facilitator
    restart: unless-stopped
    init: true
    command: ["npm", "run", "start:gamecoin-provisioner"]
    environment:
      ADX_GAMECOIN_PROVISIONER_DATABASE_URL: postgresql://adx_arena_core_login:${ADX_ARENA_CORE_DATABASE_PASSWORD:?Set ADX_ARENA_CORE_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_ARENA_SETTLEMENT_RPC_URL: ${ADX_ARENA_SETTLEMENT_RPC_URL:?Set ADX_ARENA_SETTLEMENT_RPC_URL in deploy/.env}
      ADX_CURRENT_GAME_CHAIN_ID: ${ADX_CURRENT_GAME_CHAIN_ID:-1439}
      ADX_CURRENT_GAME_TOKEN_ADDRESS: ${ADX_CURRENT_GAME_TOKEN_ADDRESS:-0xBF7B7268CE82d92BaC7a95a741F4003FE84e1884}
      ADX_GAMECOIN_PROVISIONER_POLL_SECONDS: ${ADX_GAMECOIN_PROVISIONER_POLL_SECONDS:-2}
      ADX_GAMECOIN_REQUIRED_CONFIRMATIONS: ${ADX_GAMECOIN_REQUIRED_CONFIRMATIONS:-2}
      ADX_FACILITATOR_CSV_PATH: /run/arena402/facilitators.csv
      ADX_GAMECOIN_OWNER_WALLET_INDEX: ${ADX_GAMECOIN_OWNER_WALLET_INDEX:-1}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    volumes:
      - ${ADX_FACILITATOR_CSV_HOST_PATH:-./deploy/secrets/arena402-facilitators.csv}:/run/arena402/facilitators.csv:ro
    networks:
      - edge
      - data
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "test", "-f", "/tmp/gamecoin-provisioner-ready"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s
    stop_grace_period: 30s
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  memorial-minter:
    profiles:
      - memorial
    build:
      context: .
      dockerfile: deploy/docker/Dockerfile.facilitator
    restart: unless-stopped
    init: true
    command: ["npm", "run", "start:memorial-minter"]
    environment:
      ADX_MEMORIAL_MINTER_DATABASE_URL: postgresql://adx_arena_core_login:${ADX_ARENA_CORE_DATABASE_PASSWORD:?Set ADX_ARENA_CORE_DATABASE_PASSWORD in deploy/.env}@postgres:5432/${POSTGRES_DB:-adx}
      ADX_ARENA_SETTLEMENT_RPC_URL: ${ADX_ARENA_SETTLEMENT_RPC_URL:?Set ADX_ARENA_SETTLEMENT_RPC_URL in deploy/.env}
      ADX_CURRENT_GAME_CHAIN_ID: ${ADX_CURRENT_GAME_CHAIN_ID:-1439}
      ADX_MEMORIAL_CONTRACT_ADDRESS: ${ADX_MEMORIAL_CONTRACT_ADDRESS:-0x17d9cd66b9bc7b6d12fb3c60cbf81b2f814c2364}
      ADX_MEMORIAL_MINTER_POLL_SECONDS: ${ADX_MEMORIAL_MINTER_POLL_SECONDS:-2}
      ADX_MEMORIAL_MINTER_BATCH_SIZE: ${ADX_MEMORIAL_MINTER_BATCH_SIZE:-1}
      ADX_FACILITATOR_CSV_PATH: /run/arena402/facilitators.csv
      ADX_FACILITATOR_WALLET_INDEX: ${ADX_FACILITATOR_1_WALLET_INDEX:-1}
      TZ: ${TZ:-Asia/Shanghai}
    depends_on:
      provision-db-roles:
        condition: service_completed_successfully
    volumes:
      - ${ADX_FACILITATOR_CSV_HOST_PATH:-./deploy/secrets/arena402-facilitators.csv}:/run/arena402/facilitators.csv:ro
    networks:
      - edge
      - data
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "test", "-f", "/tmp/memorial-minter-ready"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s
    stop_grace_period: 30s
    cpus: 0.20
    mem_limit: 256m
    logging: *default-logging

  caddy:
    image: caddy:2-alpine@sha256:5f5c8640aae01df9654968d946d8f1a56c497f1dd5c5cda4cf95ab7c14d58648
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
      connector-api:
        condition: service_healthy
    environment:
      ADX_PUBLIC_HOST: ${ADX_PUBLIC_HOST:?Set ADX_PUBLIC_HOST in deploy/.env}
      ADX_PUBLIC_APP_URL: ${ADX_PUBLIC_APP_URL:?Set ADX_PUBLIC_APP_URL in deploy/.env}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./deploy/caddy/${ADX_CADDY_CONFIG:-Caddyfile.domain}:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
      - certbot_config:/etc/letsencrypt:ro
      - certbot_www:/var/www/certbot:ro
      - ./deploy/artifacts:/srv/downloads:ro
    networks:
      - edge
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:2019/config/"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 15s
    cpus: 0.10
    mem_limit: 128m
    logging: *default-logging

  certbot:
    image: certbot/certbot:v5.7.0@sha256:34ee91d2f43008eb78a007d22f23ed4b2eaa9a454cb27ca2c042b49527a695b4
    profiles:
      - tools
    volumes:
      - certbot_config:/etc/letsencrypt
      - certbot_lib:/var/lib/letsencrypt
      - certbot_www:/var/www/certbot
    networks:
      - edge
    security_opt:
      - no-new-privileges:true
    cpus: 0.25
    mem_limit: 256m
    logging: *default-logging

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
  certbot_config:
  certbot_lib:
  certbot_www:

networks:
  edge:
  data:
    internal: true
  official-llm:
